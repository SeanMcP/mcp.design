import React from 'react';
import styled from 'styled-components';

const Header = styled.header`
    background: blue;
    color: white;
    width: 100%;
`

export default () => (
    <Header>Text</Header>
)